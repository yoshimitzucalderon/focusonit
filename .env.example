# =============================================================================
# SUPABASE CONFIGURATION (REQUIRED)
# =============================================================================

# Supabase project URL (public)
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://tu-supabase-url.com

# Supabase anon/public key (public, safe to expose)
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-anon-key

# Supabase service role key (SECRET - server-side only, never expose to client)
# Used for admin operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key

# =============================================================================
# GOOGLE OAUTH & CALENDAR (REQUIRED for Google features)
# =============================================================================

# Google OAuth Client ID (for Sign in with Google)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (SECRET - server-side only)
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# =============================================================================
# SENTRY ERROR TRACKING (REQUIRED for production monitoring)
# =============================================================================

# Sentry DSN (public, safe to expose)
# Get from: Sentry Dashboard → Settings → Client Keys
# Example: https://abc123def456@o123456.ingest.sentry.io/789012
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@oxxxxx.ingest.sentry.io/xxxxx

# Sentry Environment (determines which Sentry project receives events)
# Options: development, staging, production
NEXT_PUBLIC_SENTRY_ENVIRONMENT=production

# Sentry Performance Monitoring - Sample Rate
# 1.0 = 100% (all transactions), 0.1 = 10%, 0 = disabled
# Recommended: 1.0 (dev), 0.5 (staging), 0.1 (production)
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# Sentry Debug Mode (ONLY enable in development)
# Shows detailed Sentry logs in console
NEXT_PUBLIC_SENTRY_DEBUG=false

# Sentry Session Replay - Sample Rates (optional)
# Captures user sessions for debugging (consumes quota)
NEXT_PUBLIC_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1
NEXT_PUBLIC_SENTRY_REPLAYS_ERROR_SAMPLE_RATE=1.0

# =============================================================================
# SENTRY SOURCE MAPS UPLOAD (Build-time only, for readable stack traces)
# =============================================================================

# Sentry Organization slug
# Find in: Sentry Dashboard → Settings → Organization
SENTRY_ORG=your-sentry-org

# Sentry Project slug
# Find in: Sentry Dashboard → Settings → Projects
SENTRY_PROJECT=focusonit-production

# Sentry Auth Token (SECRET - for uploading source maps during build)
# Create at: Sentry → User Settings → Auth Tokens
# Required scopes: project:releases, project:write
# NOTE: Only needed during build, not at runtime
SENTRY_AUTH_TOKEN=sntrys_your_auth_token_here

# =============================================================================
# ALERTING & NOTIFICATIONS (Optional but recommended for production)
# =============================================================================

# Slack Webhook URLs for alerts (SECRET)
# Create at: https://api.slack.com/apps → Incoming Webhooks
SLACK_WEBHOOK_ALERTS=https://hooks.slack.com/services/T00/B00/xxx
SLACK_WEBHOOK_ERRORS=https://hooks.slack.com/services/T00/B00/yyy
SLACK_WEBHOOK_PERFORMANCE=https://hooks.slack.com/services/T00/B00/zzz
SLACK_WEBHOOK_DEPLOYS=https://hooks.slack.com/services/T00/B00/zzz

# =============================================================================
# N8N INTEGRATION (Optional - for voice-to-task workflow)
# =============================================================================

# n8n Webhook URL for voice-to-task processing
N8N_VOICE_TASK_WEBHOOK_URL=https://tu-instancia-n8n.com/webhook/voice-to-task

# =============================================================================
# APPLICATION CONFIG
# =============================================================================

# Application URL (used for OAuth redirects, absolute URLs)
# Development: http://localhost:3000
# Production: https://focusonit.ycm360.com
NEXT_PUBLIC_APP_URL=https://focusonit.ycm360.com

# Application Version (optional - for Sentry release tracking)
# Can use: git commit SHA, package.json version, build number
# Vercel auto-populates: VERCEL_GIT_COMMIT_SHA
NEXT_PUBLIC_APP_VERSION=1.0.0

# =============================================================================
# NOTES
# =============================================================================

# Variables with NEXT_PUBLIC_ prefix:
# - Exposed to the browser (public)
# - Can be used in client components
# - Should NOT contain secrets
# - Must be in Plaintext in Vercel (not Encrypted) if used in middleware/edge

# Variables WITHOUT prefix:
# - Server-side only
# - Never sent to browser
# - Can contain secrets
# - Use for API keys, tokens, service role keys

# Vercel Environment Variables:
# - Production: Use for prod environment
# - Preview: Use for staging/preview deployments
# - Development: Use for local development (but prefer .env.local)

# For more info:
# - Monitoring Setup: docs/monitoring/SENTRY_SETUP.md
# - Environment Variables: docs/setup/ENVIRONMENT_VARIABLES.md (if exists)
# - Vercel Deployment: docs/deployments/VERCEL_DEPLOYMENT.md